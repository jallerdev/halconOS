fastapi>=0.115.0,<1.0.0
uvicorn[standard]>=0.32.0,<1.0.0
pydantic>=2.0.0,<3.0.0
scrapegraphai>=1.32.0,<2.0.0
playwright>=1.48.0,<2.0.0
python-dotenv>=1.0.0,<2.0.0
beautifulsoup4>=4.12.0,<5.0.0
lxml>=5.0.0,<6.0.0
httpx>=0.27.0,<1.0.0

# Pin obligatorio: scrapegraphai 1.32 importa `ChatOllama` desde
# `langchain_community.chat_models`. En langchain-community 0.3.x ese símbolo
# fue movido a `langchain_ollama`, y si pip resuelve a 0.3+ el import falla
# al cargar `scrapegraphai.nodes.generate_answer_node`. Pin a 0.2.x mientras
# scrapegraphai migra. Cuando salga una versión nueva que use el import
# correcto, podemos subir scrapegraphai y quitar este pin.
langchain-community>=0.2.16,<0.3.0

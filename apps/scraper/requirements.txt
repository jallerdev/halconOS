fastapi>=0.115.0,<1.0.0
uvicorn[standard]>=0.32.0,<1.0.0
pydantic>=2.0.0,<3.0.0
# scrapegraphai 1.32 declara langchain-community>=0.3 pero su código importa
# ChatOllama desde langchain_community.chat_models (que ya no existe en 0.3+).
# Esa versión está rota. >=1.40 migró a `langchain-ollama` (paquete separado).
scrapegraphai>=1.40.0,<2.0.0
playwright>=1.48.0,<2.0.0
python-dotenv>=1.0.0,<2.0.0
beautifulsoup4>=4.12.0,<5.0.0
lxml>=5.0.0,<6.0.0
httpx>=0.27.0,<1.0.0
# Asegura que la nueva ubicación de ChatOllama esté disponible. scrapegraphai
# >=1.40 ya lo trae como dep pero lo dejamos explícito por si la resolución
# de pip elige otra cosa.
langchain-ollama>=0.2.0,<1.0.0
